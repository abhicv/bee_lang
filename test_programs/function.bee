fn main (argc: int, argv: int) : int {
    let x: int = 100 * 0;
    let y : int = 200;
    let sum = add(x, y) + add(100, 200);
    let b = 10 + 20;
    return 0;
}

fn add (x: int, y: int) : [] int {
    return x + y;
}