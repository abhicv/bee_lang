struct Pixel {
    red: u32;
    green: u32;
    blue: u32;
}

struct DrawCommand {
    command: [10] u32;
    variable_commands: [] u32;
}

fn draw_to_screen(buffer: [] Pixel) : [] Pixel {
    let mask: u32 = 0;
    let pixel = buffer[x * y + 1];
    let x = 10;
}

