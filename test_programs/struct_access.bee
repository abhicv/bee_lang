struct vector2 {
    x : int;
    y : int;
}

struct String {
    data : [] char;
    length: int;
}

fn vector2_add(a: vector2, b: vector2) : vector2 {
    let result: vector2 = 0;
    result.x = a.x + b.x; 
    result.y = a.y + b.y;
    return result;
}

fn scale_vector(v: vector2) {

}

fn main (arg_count: int, args: [] String) : int {
    let msg: String = "dsdadas";

    msg.data[10] = 'A';

    if is_playable {

    }

    let game_over: bool = false;
    
    while !game_over {
        update_and_render_frame();
    }
}
