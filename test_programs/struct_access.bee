struct vector2 {
    x : int;
    y : int;
    a: int [10];
    b: string;
}

struct String {
    data : char [1000];
    length: int;
}

fn start (arg: int [30], a : int) : int {
    let x : int [5] = 100;
    let y : int = 20;
    let msg : string = "Hey, Sailor!";
    let msg : str [10] = "Hey, Sailor!";
    
    if (a) {
        print(a);
    } else if (b) {
        print(b);
    } else {
        print(10);
    }

    let x: int [10] = 0;
    
    let n : int = 0;
    while (n < 10) {
        x[n] = n * 2;
        n = n + 1;
    }

    first.second.third = qwerty + asdfg.zxcv;
    x = asdfg[10].zxcv[20];
}