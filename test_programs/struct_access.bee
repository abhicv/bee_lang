struct vector2 {
    x : int;
    y : int;
    a: int [10];
    b: string;
}

struct String {
    data : char [1000];
    length: int;
}

fn start (arg: int [30], a : int) : int {
    let x : int [5] = 100;
    let y : int = 20;
    let msg : string = "Hey, Sailor!";
    let msg : str [10] = "Hey, Sailor!";
    
    while (x < y) {
        x = x + 1;
    }

    if (a) {
        print(a);
    } else if (b) {
        print(b);
    } else {
        print(10);
    }
}